spring:
  profiles:
    include: oauth, jwt
  security:
    user:
      password: 1
  datasource:
    url: jdbc:h2:mem:test;NON_KEYWORDS=USER;INIT=CREATE SCHEMA IF NOT EXISTS ENGLISH_STUDY_WEB\;CREATE SCHEMA IF NOT EXISTS ENGLISH_STUDY_WEB
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: create
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/portfolio
    username: user
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: update